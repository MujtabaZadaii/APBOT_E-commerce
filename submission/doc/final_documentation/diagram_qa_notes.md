# Diagram QA Notes

The rendered system architecture diagram is a wide, readable flow showing Customer/Browser, React/Vite Frontend, Node/Express Backend, Python/Flask AI Service, Keras model, and MongoDB. Endpoint labels and the fallback rule path are visible.

The rendered dialog flow diagram is a tall, readable flow showing welcome, text/voice input, Python intent prediction, confidence threshold, Express context resolution, product/search/recommendation/FAQ/tracking/size/checkout branches, mock payment/order creation, fallback guidance, UI rendering, and conversation continuation.

Both diagram assets are suitable for insertion into the Word report and are included with editable Mermaid source files.
